// Token to access with user permision (Config in https://www.notion.so/my-integrations)
export const notionToken = process.env.NOTION_TOKEN || ""

// Workspace database id
export const databaseId = process.env.NOTION_DATABASE_ID || ""

// Blog data source id
export const blogDataSourceId = process.env.NOTION_BLOGS_DATA_SOURCES_ID || ""
